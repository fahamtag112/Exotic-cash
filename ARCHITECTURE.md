# 🏗️ EXOTIC CASH - System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        BROWSER (Client)                         │
│                    http://localhost:5173                         │
└────────────────┬─────────────────────────────────────────────────┘
                 │
                 │ HTTP/AJAX Requests
                 │
        ┌────────▼─────────┐
        │   FRONTEND APP   │
        │  (React + Vite)  │
        └────────┬─────────┘
                 │
        ┌────────▼─────────────────────────┐
        │    Router (React Router)         │
        │  ├─ /                (Login)     │
        │  ├─ /home            (Home)      │
        │  ├─ /admin-dashboard (Protected) │
        │  └─ /user-dashboard  (Protected) │
        └────────┬─────────────────────────┘
                 │
                 │ API Calls
                 │
    ┌────────────▼────────────┐
    │   REST API Calls        │
    │  (POST, GET, DELETE)    │
    └────────────┬────────────┘
                 │
                 │ Port 5000
                 │
    ┌────────────▼──────────────────────────────────┐
    │          EXPRESS BACKEND SERVER               │
    │        http://localhost:5000                  │
    └────────────┬──────────────────────────────────┘
                 │
        ┌────────▼────────────────┐
        │   API Routes            │
        │  ├─ POST /auth/login    │
        │  ├─ POST /auth/register │
        │  └─ GET  /auth/me       │
        └────────┬────────────────┘
                 │
        ┌────────▼────────────────┐
        │   Middleware            │
        │  ├─ JWT Verification    │
        │  ├─ Role Checking       │
        │  └─ Error Handling      │
        └────────┬────────────────┘
                 │
                 │ SQL Queries
                 │
    ┌────────────▼──────────────────────────────────┐
    │         POSTGRESQL DATABASE                   │
    │        (exotic_cash_db)                       │
    │                                               │
    │  ┌──────────────────────────────────────┐   │
    │  │         USERS TABLE                  │   │
    │  │  ├─ id (PRIMARY KEY)                 │   │
    │  │  ├─ admin_id (UNIQUE)                │   │
    │  │  ├─ password_hash (BCRYPT)           │   │
    │  │  ├─ role (admin | user)              │   │
    │  │  ├─ full_name                        │   │
    │  │  ├─ email                            │   │
    │  │  └─ timestamps                       │   │
    │  └──────────────────────────────────────┘   │
    │                                               │
    └───────────────────────────────────────────────┘
```

---

## Authentication Flow Diagram

```
┌──────────────┐
│   User       │
│  (Browser)   │
└──────┬───────┘
       │
       │ 1. Enters AdminId & Password
       ▼
┌─────────────────────┐
│  Login Form         │
│  - Admin ID input   │
│  - Password input   │
└──────┬──────────────┘
       │
       │ 2. Submit Form
       ▼
┌─────────────────────────────────────┐
│  Frontend Validation                │
│  - Check inputs not empty           │
│  - Basic format validation          │
└──────┬──────────────────────────────┘
       │
       │ 3. POST /api/auth/login
       ▼
┌──────────────────────────────────────────┐
│  Express Backend                         │
│  - Receive credentials                   │
│  - Query PostgreSQL users table          │
└──────┬───────────────────────────────────┘
       │
       │ 4. Database Query
       ▼
┌──────────────────────────────────────────┐
│  PostgreSQL                              │
│  - Find user by admin_id                 │
│  - Return user record with password_hash │
└──────┬───────────────────────────────────┘
       │
       │ 5. Password Verification
       ▼
┌──────────────────────────────────────────┐
│  Bcrypt Comparison                       │
│  - Compare submitted password            │
│  - With stored hash                      │
│  ├─ Match? → Continue                    │
│  └─ No match? → Return error             │
└──────┬───────────────────────────────────┘
       │
       │ 6. Role Detection
       ▼
┌──────────────────────────────────────────┐
│  Check User Role                         │
│  ├─ role === 'admin'?                    │
│  │  └─ Create Admin Token                │
│  └─ role === 'user'?                     │
│     └─ Create User Token                 │
└──────┬───────────────────────────────────┘
       │
       │ 7. JWT Generation
       ▼
┌──────────────────────────────────────────┐
│  Generate JWT Token                      │
│  - Payload: { id, admin_id, role }       │
│  - Secret: JWT_SECRET                    │
│  - Expires: 24 hours                     │
└──────┬───────────────────────────────────┘
       │
       │ 8. Response with Token
       ▼
┌─────────────────────────────────────────────┐
│  Return Success Response                    │
│  {                                          │
│    "success": true,                         │
│    "token": "eyJhbGci...",                 │
│    "user": {                                │
│      "id": 1,                               │
│      "admin_id": "Admin112",                │
│      "role": "admin",                       │
│      "full_name": "Administrator",          │
│      "email": "admin@exoticcash.com"        │
│    },                                       │
│    "message": "Admin login successful"      │
│  }                                          │
└──────┬──────────────────────────────────────┘
       │
       │ 9. Frontend Storage
       ▼
┌──────────────────────────────────────────────────┐
│  localStorage.setItem('token', token)            │
│  localStorage.setItem('user', JSON.stringify())  │
└──────┬───────────────────────────────────────────┘
       │
       │ 10. Route Decision
       ▼
┌──────────────────────────────────────────┐
│  Check User Role                         │
│  ├─ if (role === 'admin')                │
│  │  └─ → /admin-dashboard                │
│  └─ if (role === 'user')                 │
│     └─ → /user-dashboard                 │
└──────┬───────────────────────────────────┘
       │
       │ 11. Redirect
       ▼
┌──────────────────────────────────────────┐
│  Display Dashboard                       │
│  ├─ Admin Dashboard                      │
│  │  └─ Statistics, Users, Transactions   │
│  └─ User Dashboard                       │
│     └─ Balance, Transactions, Settings   │
└──────────────────────────────────────────┘
```

---

## Component Architecture

```
App.tsx (Main Router)
│
├─ Public Routes
│  ├─ / (Login Page)
│  │  └─ Login.tsx
│  │     ├─ Login Form
│  │     ├─ Register Form Toggle
│  │     └─ Demo Credentials
│  │
│  └─ /home (Home Page)
│     └─ Index.tsx
│        ├─ Header
│        ├─ Hero Section
│        └─ Features Grid
│
└─ Protected Routes (Role-Based)
   ├─ /admin-dashboard (Admin Only)
   │  └─ AdminDashboard.tsx
   │     ├─ Header with Logout
   │     ├─ Sidebar Navigation
   │     ├─ Statistics Section
   │     ├─ Activities Table
   │     └─ Quick Actions
   │
   └─ /user-dashboard (User Only)
      └─ UserDashboard.tsx
         ├─ Header with Logout
         ├─ Balance Card
         ├─ Account Overview
         ├─ Transactions Table
         ├─ Quick Actions
         └─ Info Sidebar
```

---

## Data Flow Diagram

```
┌────────────────────┐
│   User Input       │
│  (Credentials)     │
└─────────┬──────────┘
          │
          │ AdminId: "Admin112"
          │ Password: "Admin@112"
          │
          ▼
┌────────────────────┐
│   Frontend Form    │
│   Validation       │
└─────────┬──────────┘
          │
          │ Valid Input
          │
          ▼
┌────────────────────────────────────────┐
│ POST /api/auth/login                   │
│ ├─ admin_id: "Admin112"                │
│ └─ password: "Admin@112"               │
└─────────┬────────────────────────────────┘
          │
          ▼
┌────────────────────────────────────────────┐
│ Database Query                             │
│ SELECT * FROM users WHERE admin_id = ... │
└─────────┬────────────────────────────────────┘
          │
          │ User Record
          │ {
          │   id: 1,
          │   admin_id: "Admin112",
          │   password_hash: "$2b$10$qRVRHYu0j.gBmVsyB76wwuffzqLhFp4...",
          │   role: "admin",
          │   full_name: "Administrator",
          │   email: "admin@exoticcash.com"
          │ }
          │
          ▼
┌────────────────────────────┐
│ Bcrypt Hash Comparison     │
│ bcrypt.compare(            │
│   "Admin@112",            │
│   "$2b$10$qRVRHYu0j..."    │
│ )                          │
└─────────┬──────────────────┘
          │
          │ Match? YES
          │
          ▼
┌────────────────────────────────────────┐
│ JWT Token Generation                   │
│ jwt.sign(                              │
│   {                                    │
│     id: 1,                             │
│     admin_id: "Admin112",              │
│     role: "admin"                      │
│   },                                   │
│   JWT_SECRET,                          │
│   { expiresIn: "24h" }                 │
│ )                                      │
└─────────┬────────────────────────────────┘
          │
          │ Token: "eyJhbGciOiJIUzI1NiIs..."
          │
          ▼
┌────────────────────────────────────────┐
│ API Response                           │
│ {                                      │
│   success: true,                       │
│   token: "...",                        │
│   user: {                              │
│     id: 1,                             │
│     role: "admin",                     │
│     ...                                │
│   }                                    │
│ }                                      │
└─────────┬────────────────────────────────┘
          │
          │ Store in localStorage
          │
          ▼
┌────────────────────────────────────────┐
│ Frontend State Update                  │
│ - Save token                           │
│ - Save user data                       │
│ - Detect role: "admin"                 │
└─────────┬────────────────────────────────┘
          │
          │ role === "admin"?
          │
          ▼
┌────────────────────────────────────────┐
│ Route Navigation                       │
│ window.location.href =                 │
│ "/admin-dashboard"                     │
└─────────┬────────────────────────────────┘
          │
          ▼
┌────────────────────────────────────────┐
│ Admin Dashboard Rendered               │
│ - Statistics                           │
│ - User Management                      │
│ - Transaction Monitoring               │
│ - Security Controls                    │
└────────────────────────────────────────┘
```

---

## Security Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                         │
└────────────────────────────────────────────────────────────┘

Layer 1: Input Validation
├─ Frontend: Basic format checking
└─ Backend: Server-side validation
   └─ Prevents malicious input

Layer 2: Password Security
├─ Hashing: Bcrypt with 10 rounds
├─ Storage: Never store plain text
└─ Comparison: Secure bcrypt.compare()

Layer 3: Authentication
├─ JWT Tokens with secret key
├─ Token expiration: 24 hours
└─ Secure token transmission

Layer 4: Authorization
├─ Role-based access control
├─ Route protection
└─ Request verification

Layer 5: Data Protection
├─ Parameterized queries
├─ SQL injection prevention
└─ Secure database connection

Layer 6: Communication
├─ HTTPS ready (for production)
├─ CORS protection
└─ Secure headers
```

---

## Technology Integration

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  REACT (UI Framework)                                       │
│  ├─ Components (Login, Dashboard)                           │
│  ├─ State Management (useState)                             │
│  └─ Lifecycle (useEffect)                                   │
│        │                                                    │
│        ├───→ REACT ROUTER (Navigation)                      │
│        │     ├─ Routes                                      │
│        │     ├─ Protected Routes                            │
│        │     └─ Role-based Redirects                        │
│        │                                                    │
│        ├───→ TYPESCRIPT (Type Safety)                       │
│        │     ├─ Component Props Types                       │
│        │     ├─ User Interface Types                        │
│        │     └─ API Response Types                          │
│        │                                                    │
│        ├───→ VITE (Build Tool)                              │
│        │     ├─ Hot Module Replacement                      │
│        │     ├─ Fast Rebuilds                               │
│        │     └─ Optimized Production Build                  │
│        │                                                    │
│        └───→ CSS3 (Styling)                                 │
│              ├─ Gradients                                   │
│              ├─ Animations                                  │
│              └─ Responsive Design                           │
│                                                             │
│  ↓ HTTP/AJAX                                                │
│                                                             │
│  EXPRESS (Backend Framework)                                │
│  ├─ Routing                                                 │
│  ├─ Middleware                                              │
│  ├─ Error Handling                                          │
│  └─ CORS Support                                            │
│        │                                                    │
│        ├───→ JWT (Authentication)                           │
│        │     ├─ Token Generation                            │
│        │     ├─ Token Verification                          │
│        │     └─ Expiration Handling                         │
│        │                                                    │
│        ├───→ BCRYPTJS (Password Hashing)                    │
│        │     ├─ Hash Generation                             │
│        │     └─ Hash Comparison                             │
│        │                                                    │
│        ├───→ POSTGRESQL (Database Driver)                   │
│        │     ├─ Connection Pooling                          │
│        │     ├─ Query Execution                             │
│        │     └─ Result Handling                             │
│        │                                                    │
│        └───→ TYPESCRIPT (Type Safety)                       │
│              ├─ Interface Definitions                       │
│              ├─ Request Types                               │
│              └─ Database Types                              │
│                                                             │
│  ↓ SQL                                                      │
│                                                             │
│  POSTGRESQL (Database)                                      │
│  ├─ Users Table                                             │
│  ├─ ENUM Types (admin|user)                                 │
│  ├─ Indexes                                                 │
│  └─ Connection Pool                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                          │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   CLIENT BROWSER    │
│  (Any Device)       │
└──────────┬──────────┘
           │
           │ HTTPS (Port 443)
           │
┌──────────▼──────────────────────────────────────┐
│         NGINX REVERSE PROXY                     │
│  ├─ SSL/TLS Termination                        │
│  ├─ Caching                                     │
│  └─ Load Balancing                             │
└──────────┬──────────────────────────────────────┘
           │
      ┌────┴─────┐
      │           │
      ▼           ▼
  ┌────────┐  ┌────────┐
  │ Node 1 │  │ Node 2 │  (Scaled)
  │Instance│  │Instance│
  │Port 5000│ │Port 5000│
  └────┬───┘  └────┬───┘
       │           │
       └─────┬─────┘
             │
      ┌──────▼──────────────┐
      │  DATABASE SERVER    │
      │  PostgreSQL Cluster │
      │  (Replicated)       │
      │  (Backup)           │
      └─────────────────────┘
```

---

This architecture provides:
- ✅ Scalability
- ✅ Security
- ✅ Reliability
- ✅ Performance
- ✅ Maintainability
