# ============================================
# EXOTIC CASH INVESTMENT PLATFORM
# Environment Configuration File
# ============================================

# NODE ENVIRONMENT
# Options: development, production, test
NODE_ENV=development

# SERVER CONFIGURATION
PORT=5000
HOST=localhost

# DATABASE CONFIGURATION
# Make sure PostgreSQL is running before starting the server
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=exotic_cash_db

# JWT CONFIGURATION
JWT_SECRET=your-secret-key-change-this-in-production-minimum-32-characters
JWT_EXPIRES_IN=7d

# EMAIL CONFIGURATION (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@exoticcash.com

# DATABASE CONNECTION POOL SETTINGS
# These are automatically configured in connection.ts
# But can be overridden here if needed
DB_POOL_MIN=5
DB_POOL_MAX=20
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=5000

# API ENDPOINTS
API_URL=http://localhost:5000
FRONTEND_URL=http://localhost:5173

# LOGGING
LOG_LEVEL=info

# IMPORTANT NOTES:
# 1. Change JWT_SECRET in production to a secure random string
# 2. Ensure PostgreSQL is running: sudo service postgresql start
# 3. Create database: createdb exotic_cash_db
# 4. Run migrations: npm run db:migrate
# 5. For production, set NODE_ENV=production and use secure credentials
